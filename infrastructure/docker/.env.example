# Copy this file to .env and fill in real values
# Never commit .env to git

# SQL Server SA password (min 8 chars, upper + lower + digit + symbol)
MSSQL_SA_PASSWORD=Str0ng!Pass2024

# AI Engine settings
API_KEY=dev-secret-key
DEBUG=false
